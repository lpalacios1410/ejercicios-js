//Primivitos - string, number, boolean, undefined, object(null), Symbol

let nombre = 'Iron man';
console.log (nombre);

nombre = 'tony stark';
console.log (nombre);

nombre = 'Jarvis';
console.log (typeof nombre); // Se puede usar para detectar que tipo de dato es la variable
